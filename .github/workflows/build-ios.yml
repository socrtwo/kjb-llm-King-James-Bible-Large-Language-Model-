# ─────────────────────────────────────────────────────────────────────────────
# Build iOS IPA using Expo / EAS Build
# ─────────────────────────────────────────────────────────────────────────────
name: Build – iOS

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      profile:
        description: "EAS build profile"
        required: false
        default: "preview"
        type: choice
        options:
          - preview
          - production

permissions:
  contents: write

env:
  WORKING_DIR: mobile

jobs:
  ios:
    runs-on: macos-latest
    defaults:
      run:
        working-directory: ${{ env.WORKING_DIR }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Set up Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Configure API URL
        run: |
          echo "EXPO_PUBLIC_API_URL=${{ vars.API_URL || 'https://api.kjbllm.app' }}" >> $GITHUB_ENV

      - name: Build iOS (preview – simulator)
        if: ${{ github.event.inputs.profile != 'production' }}
        run: eas build --platform ios --profile preview --non-interactive --local

      - name: Build iOS (production – device)
        if: ${{ github.event.inputs.profile == 'production' }}
        run: eas build --platform ios --profile production --non-interactive --local

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: |
            mobile/*.ipa
            mobile/*.tar.gz

      - name: Attach to GitHub release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            mobile/*.ipa
